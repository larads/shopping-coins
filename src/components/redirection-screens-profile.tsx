import { TouchableOpacity, TouchableOpacityProps } from "react-native";

interface ButtonProps extends TouchableOpacityProps {

}

export default function RedirectionScreenProfile({ }: ButtonProps) {
    return (
        <TouchableOpacity>

        </TouchableOpacity>
    )
}